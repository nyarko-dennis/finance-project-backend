#Add dev configuration here
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/firefox
    username: firefox_pg
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect